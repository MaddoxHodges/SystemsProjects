#include <stdio.h>
#include <stlib.h>
#include <string.h>

{
  if (*node == NULL)
  {
    *node = makeNode(y);
    return 1;
   }
  if ((*node)->val<y) return insertBST(&((*node)->left), y);
}

int SearchBST(struct Node 
int main() 
{
  

  




  
}
